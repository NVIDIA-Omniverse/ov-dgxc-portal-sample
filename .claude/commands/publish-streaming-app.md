---
description: Publish or update a streaming application on the Omniverse portal by collecting app metadata, authenticating against the portal's identity setup, and calling `PUT /api/apps/{app_id}`. Use when you need to add, publish, register, or update a streaming function or application on the portal and you can provide an NVCF `function_id` and `function_version_id`.
allowed-tools: Bash, WebFetch
---

Follow the skill at [.agents/skills/publish-streaming-app/SKILL.md](../../.agents/skills/publish-streaming-app/SKILL.md).
