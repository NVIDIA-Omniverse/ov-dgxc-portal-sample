---
description: Remove a published streaming application from the Omniverse portal by locating the app via `app_id` or via `function_id` plus `function_version_id`, authenticating against the portal's identity setup, previewing the matched app, and calling `DELETE /api/apps/{app_id}` only after explicit user approval. Use when you need to unpublish, delete, or remove a streaming application from the portal.
allowed-tools: Bash, WebFetch
---

Follow the skill at [.agents/skills/remove-streaming-app/SKILL.md](../../.agents/skills/remove-streaming-app/SKILL.md).
